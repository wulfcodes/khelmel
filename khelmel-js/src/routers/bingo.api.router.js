const express = require('express');
const router = express.Router();
const BingoApiController = require('../controllers/bingo.api.controller');

// Bingo API endpoints will be added here

module.exports = router;
